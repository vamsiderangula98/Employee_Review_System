<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <link rel="stylesheet" href="/css/admin.css">

    <title>Admin</title>
</head>
<body>
    <%- include('_header') %>

    <div class="f">
        <!-- <div>
            head part
        </div> -->
        <div class="w-40 mt-5 p-3 border border-dark">
            <div class="mx-auto mt-3" style="width: 400px;">
                <h2>Assign Work To Employees</h2>
            </div>
           <div class="w-8 m-4 p-4">
            <form method="POST" action="/admin/employee-task">
                <div class="mb-5">
                    <h4>Recipients</h4>
                    <select class="form-select" aria-label="Default select example" name="recipient">
                        <option selected disabled>Select Recipients</option>
                        <% for(u of users){ %>
                           <option value="<%= u.id %>"><%= u.name %></option>                          
                        <% } %>
                    </select>
                </div>

                <div>
                    <h4> Reviewer</h4>
                    <select class="form-select" aria-label="Default select example" name="reviewer">
                        <option selected disabled>Select Reviewer</option>
                        <% for(u of users){ %>
                           <option value="<%= u.id %>"><%= u.name %></option>                          
                        <% } %>
                    </select>
                </div>
                <div class="mx-auto mt-5 m-n" style="width: 100px;">
                    <button type="submit" class="btn btn-dark">Assign</button>
                </div>
           </form>
           </div>

        </div>
        <div class="w-40 mt-5 p-3 border border-dark">
            <div class="mx-auto mt-3" style="width: 200px;">
                <h2> Make Admin</h2>
            </div>
            
            <div class="w-8 m-4 mt-5 p-4">
                <form method="POST" action="/admin/make_admin">
                    <select class="form-select" aria-label="Default select example" name="admin">
                        <option selected>Make Admin</option>
                        <% for(u of users){ %>
                            <option value="<%= u.id %>"><%= u.name %></option>                          
                         <% } %>
                    </select>
                    <div class="mx-auto mt-7" style="width: 130px;">
                        <button type="submit" class="btn btn-dark">Make Admin</button>
                    </div>
                    
                </form>
            </div>
        </div>

    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>

</body>
</html>